実行計画とは何か？
DBMSを利用して、データベースから情報を取得・更新するときにクエリを実行します。
このクエリがデータベースで行った処理内容が実行計画です。
これを確認することで、データベースの情報を取得・更新するする際に効率のよい記述を実現することができます。

EXPLAIN
SELECT * FROM salaries
WHERE salary = '70575';
実行時間  0.00sec

EXPLAIN
SELECT * FROM salaries
WHERE emp_no = '10100';

上記二つのクエリによる処理速度の違い
・条件句によるインデックス利用の有無 --- 今回ではインデックス(プライマリーキー)を利用した等価検索
・インデックスが利用できていて、検索範囲がプライマリーキーに限定できている
・さらにプライマリーキーに対して定数を指定できているため、より処理範囲を狭くできる
